version: '3.8'
services:
  web:
    build: .
    ports:
      - "8080:5000"
    environment:
      - SECRET_KEY=change-me-in-production-2024
    volumes:
      - quiz_data:/app/instance
    restart: unless-stopped
volumes:
  quiz_data:
